pipeline {
    agent any
    stages {
        stage('Hello from GitHub') {
            steps {
                echo 'Jenkins triggered from GitHub push!'
                sh 'whoami'
                sh 'date'
                echo 'if you are seing this message means that our github commit and auto develope is successful '
                echo 'my mistake i typed sh first instead of echo and now i think it will work like a charm'
                echo 'we are just using echo commands and i am geting bored bro i think we should do move on to next '
                echo ' i am in the college network and now i am  testing it will change or not wheather the build iwll be succful or failure'
                echo 'so this is 6 the time we are edting the file i am gussing lets see if this build automatically or not '
            }
        }
    }
}
